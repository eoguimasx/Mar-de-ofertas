// Runtime environment example.
// In production, generate `env.js` at build time with your environment values.
// Example content (do NOT commit real keys):

window.__ENV__ = {
  SUPABASE_URL: 'https://your-project.supabase.co',
  SUPABASE_ANON_KEY: 'your-anon-key-here'
};
